name: Build App
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print current directory
        run: pwd
      - name: List directories
        run: ls
#        uses: actions/checkout@v2
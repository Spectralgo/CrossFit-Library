name: Build App
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull code
        uses: actions/checkout@v2
      - name: Print current directory
        run: pwd
      - name: List directories
        run: ls